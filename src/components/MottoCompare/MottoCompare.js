import "./MottoCompare.scss" //Imports css file to its component.

export default function MottoCompare() {
    return (
    <nav className="motto-compare">

        <div>
            <div className="motto-compare__content">
                <div> <b>COMPARE NEIGHBORHOODS</b></div>
                <div> <i>Select neighborhoods and compare them side by side.</i></div>
            </div>
        </div>

    </nav>
    )
}
